export const styles = (theme) => ({
  wrapper: {
    position: "relative",
    minHeigth: "calc(100vh - 24px * 72px)",
  },
  fab: {
    position: "absolute",
    bottom: 0,
    right: 0,
    // top: 10,
    // right: 10,
  },
});
