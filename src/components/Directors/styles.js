export const styles = (theme) => ({
  wrapper: {
    position: "relative",
    minheigth: "calc(100vh - 24px - 2 * 72px)",
  },
  fab: {
    position: "absolute",
    bottom: -24,
    left: 0,
  },
});
