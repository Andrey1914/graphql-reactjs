export const styles = (theme) => ({
  wrapper: {
    position: "relative",
    minheight: "calc(100vh - 24px * 2 - 72px)",
  },
  fab: {
    position: "absolute",
    bottom: -24,
    right: 0,
  },
});
