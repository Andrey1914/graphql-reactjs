export const styles = () => ({
  wrapper: {
    position: "relative",
    minHeight: "calc(100vh - 24px * 2 - 72px)",
  },
  fab: {
    position: "absolute",
    // bottom: 0,
    top: 10,
    right: 10,
  },
});
